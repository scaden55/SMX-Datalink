// Dispatch overlay content is now rendered by SharedMapContainer → DispatchOverlay.
// This page component exists only to satisfy the route definition in App.tsx.

export default function DispatchMapPage() {
  return null;
}
