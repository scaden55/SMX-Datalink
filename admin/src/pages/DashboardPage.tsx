// Overview overlay content is now rendered by SharedMapContainer → OverviewOverlay.
// This page component exists only to satisfy the route definition in App.tsx.

export function DashboardPage() {
  return null;
}

export default DashboardPage;
